#include<stdio.h>
int main()
{
char str1[50];
char str2[50];
int i,l1=0,l2=0,flag;
printf("enter the 1st string\n");
gets(str1);
printf("enter the 2nd string\n");
gets(str2);
for(i=0;str1[i]!='\0';i++)
{l1=l1+1;}
printf("length of string %d\n",l1);

for(i=0;str2[i]!='\0';i++)
{l2=l2+1;}
printf("length of string %d\n",l2);

if(l1==l2)
{printf("l1 and l2 are equal lengths\n");}
else
{printf("l1 and l2 are not equal lengths\n");}

for(i=0;str1[i]!='\0';i++)
{ if (str1[i]!=str2[i])
{flag=1;
}
}
 if(flag==1)
 {printf("strings are not the same\n");}
 if(flag==0)
 {printf("strings are the same\n");}

}
